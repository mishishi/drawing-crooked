# 画传歪了 — MVP 开发指令

> 完整技术方案与需求文档，直接丢给 Claude Code 开始开发

---

## 一、游戏概述

**游戏名**：画传歪了
**类型**：多人联机派对整活游戏
**核心体验**：每人画一笔，越传越歪，笑点来自于传递过程中的脑洞误解
**目标平台**：浏览器（PC+手机）+ Electon 桌面端 + 微信小程序（后期）

---

## 二、核心玩法流程

### 一局完整流程

1. **4-8人** 进入同一房间，好友或随机匹配
2. 系统给**每位玩家随机派发一句抽象短句**（看不到其他人收到的句子）

   示例句子库：
   ```
   下雨天偷偷遛猫、凌晨三点买烤肠、外星人逛菜市场、
   工位偷偷摸鱼、甲方爸爸改需求、地铁老人看手机、
   猫在键盘上跳舞、老板突然出现在身后
   ```

3. **限时30秒**：只能画图，禁止写任何文字/数字/字母，把句子画出来
4. 画完**自动传给下一位玩家**（看不到原句，只能看前一个人的画）
5. 下一位玩家根据看到的画，**猜出意思后重新画一张**，传给下一个人
6. 循环传递**一圈**（8人传8次），游戏结束
7. **全量揭晓**：展示原句 + 每轮被画歪的版本
8. **投票环节**：选出最离谱画作、最脑洞脑补、最离谱跑偏奖，结算积分

---

## 三、技术方案

### 前端

- **技术栈**：HTML5 Canvas + Vue3（或 React）
- **画布**：原生 Canvas API，简单画笔（画笔/橡皮/粗细/颜色）+ 色块填充
- **实时同步**：Socket.io-client
- **适配**：响应式，PC 和手机浏览器都能玩

### 后端（Node.js）

- **技术栈**：Node.js + Express + Socket.io
- **房间状态**：内存存储（Redis 可选，后期再加）
- **题库**：JSON 文件存储，内置 200+ 句子
- **部署**：支持 Nginx 反向代理，适配国内服务器

### 架构

```
[玩家浏览器] ←WebSocket→ [Node.js 服务端] ←→ [房间状态 in-memory]
                                        ↓
                                   [句子题库 JSON]
```

---

## 四、功能模块

### 4.1 房间系统

| 功能 | 说明 |
|------|------|
| 创建房间 | 生成 6 位房间号，返回房间链接 |
| 加入房间 | 输入房间号或点击链接进入 |
| 房间列表 | 显示当前在线房间（可选） |
| 房主踢人 | 房主可以踢出玩家 |
| 准备/开始 | 所有人点准备后，房主开始游戏 |
| 结算展示 | 所有人画作和原句揭晓 |

### 4.2 画布系统

| 功能 | 说明 |
|------|------|
| 画笔工具 | 可选画笔/橡皮 |
| 画笔粗细 | 3 档：小/中/大 |
| 画笔颜色 | 8 种基础色 + 白色 |
| 清空重画 | 一键清空画布 |
| 画布同步 | 每次笔画结束自动同步到下一个玩家 |
| 剩余时间 | 倒计时 30 秒，超时自动提交当前画作 |

### 4.3 游戏逻辑

| 功能 | 说明 |
|------|------|
| 句子派发 | 每个玩家随机拿一句，不重复 |
| 传递顺序 | 固定顺时针传递，记录每轮画作 |
| AI 辅助（后期） | AI 识别画作猜意思（MVP 暂不实现） |
| 回合超时 | 超时自动提交当前画作进入下一轮 |
| 原句揭晓 | 所有人画完后，展示每人的原始句子 |

### 4.4 投票系统

| 功能 | 说明 |
|------|------|
| 投票选项 | 每轮每人投 1 票（给自己不能投） |
| 票数统计 | 服务器实时统计 |
| 积分累计 | 每轮投票积分累加到游戏结束 |
| 称号奖励 | 游戏结束颁发称号（最多票玩家） |

### 4.5 称号系统

```
歪画大师 — 画作最离谱
脑补王者 — 猜词最脑洞
灵魂画手 — 技术分最高（线条流畅度）
带偏之王 — 传话带偏最多人
```

---

## 五、数据结构（简要）

### 房间状态

```json
{
  "roomId": "ABC123",
  "players": [
    { "id": "socket_id", "name": "玩家1", "ready": true, "score": 0 }
  ],
  "owner": "socket_id",
  "status": "waiting",  // waiting | playing | voting | ended
  "currentRound": 0,
  "totalRounds": 8,
  "sentences": { "socket_id": "原句" },
  "drawings": [  // 每轮的画作历史
    { "from": "socket_id", "to": "socket_id", "imageData": "base64..." }
  ]
}
```

### 句子题库格式

```json
[
  { "id": 1, "text": "下雨天偷偷遛猫", "tags": ["日常", "宠物"] },
  { "id": 2, "text": "凌晨三点买烤肠", "tags": ["日常", "深夜"] }
]
```

---

## 六、Socket.io 事件设计

### 客户端 → 服务端

| 事件名 | _payload_ | 说明 |
|--------|-----------|------|
| `create-room` | `{ playerName }` | 创建房间 |
| `join-room` | `{ roomId, playerName }` | 加入房间 |
| `leave-room` | `{}` | 离开房间 |
| `player-ready` | `{}` | 玩家准备 |
| `start-game` | `{}` | 房主开始游戏 |
| `draw-stroke` | `{ imageData }` | 单笔画画数据 |
| `submit-drawing` | `{}` | 确认提交画作 |
| `vote` | `{ targetPlayerId }` | 投票 |

### 服务端 → 客户端

| 事件名 | _payload_ | 说明 |
|--------|-----------|------|
| `room-joined` | `{ room, playerId }` | 加入房间成功 |
| `player-joined` | `{ player }` | 新玩家加入 |
| `player-left` | `{ playerId }` | 玩家离开 |
| `game-started` | `{ sentences, currentPlayer }` | 游戏开始 |
| `your-sentence` | `{ sentence }` | 收到自己的句子 |
| `new-round` | `{ round, currentPlayer, drawing }` | 新一轮开始 |
| `drawing-update` | `{ imageData }` | 画布更新 |
| `round-ended` | `{}` | 本轮结束 |
| `game-ended` | `{ results }` | 游戏结束，揭晓结果 |
| `vote-started` | `{ candidates }` | 投票开始 |
| `vote-ended` | `{ results }` | 投票结束 |

---

## 七、UI/UX 简要说明

### 页面结构

1. **首页**：游戏标题 + 创建房间 + 加入房间 + 房间号输入框
2. **等待室**：房间号 + 玩家列表 + 准备状态 + 开始按钮（房主可见）
3. **画画页**：画布 + 工具栏 + 倒计时 + 提示（"画出你看到的"或"画出这个句子"）
4. **传递页**：等待下一个人画画的过渡动画
5. **揭晓页**：所有画作+原句的展示，滚动查看
6. **投票页**：每人投一票，选出各奖项
7. **结算页**：积分排名 + 称号颁发 + 再来一局

### 配色

```
背景：#FAFAFA（浅灰白）
主色：#FF6B6B（珊瑚红）
强调：#4ECDC4（薄荷绿）
文字：#2C3E50（深灰蓝）
```

### 风格

白板涂鸦风，简约卡通线条，无复杂场景，干净治愈沙雕风。

---

## 八、开发顺序（建议）

### Phase 1 — 基础跑通
1. 搭建 Node.js + Express + Socket.io 项目
2. 实现房间创建/加入（无画画）
3. 实现简单消息广播

### Phase 2 — 核心玩法
4. 画布基础功能（画笔、橡皮、颜色、粗细）
5. 画布数据同步
6. 句子派发和传递逻辑

### Phase 3 — 完整流程
7. 超时处理
8. 投票系统
9. 结果揭晓页面

### Phase 4 — 上线准备
10. 响应式适配（手机浏览器）
11. 房间状态持久化（刷新不掉线）
12. 部署到服务器

---

## 九、注意事项

1. **禁止文字**：画画过程中任何时候都不能出现文字/字母/数字，发现作弊该轮画作作废
2. **超时处理**：30秒倒计时内必须提交，超时自动提交当前画面
3. **中途退出**：玩家退出后，该位置空缺，游戏可继续但不影响其他人
4. **房主离开**：房主离开后，自动转移给第二个进入的玩家

---

## 十、题库示例（50句）

```
下雨天偷偷遛猫
凌晨三点买烤肠
外星人逛菜市场
工位偷偷摸鱼
甲方爸爸改需求
地铁老人看手机
猫在键盘上跳舞
老板突然出现在身后
麦当劳无人收银
程序员删库跑路
猫皇登基铲屎官
爸妈翻我聊天记录
室友半夜吃螺蛳粉
数学考试选择题全蒙B
体育老师突然要晨跑
外卖小哥走错楼
奶奶非要给我算命
理发店tony老师
眼镜在鼻梁上滑滑梯
手机掉进马桶捞起来
```

---

> 以上为完整 MVP 需求文档，可直接交给 Claude Code 开发。
> 建议 Claude 使用 cursor 或 Windsurf 等 AI IDE 辅助开发效率更高。